name: Dotnet workflow fixture

on:
  workflow_dispatch:

permissions: {}

jobs:
  test:
    uses: ./.github/workflows/wf-test-dotnet.yml
    permissions:
      contents: read
    with:
      dotnet-version: 10.0.x
      target: tests/fixtures/mock-projects/dotnet/Mock.Container.App.Tests/Mock.Container.App.Tests.csproj
      postgres-enabled: true
      postgres-image: postgres:17.4
      postgres-host-port: 55432
